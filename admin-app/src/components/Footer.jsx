import React from 'react';
import PropTypes from 'prop-types';

const Footer = props => {
  return (
    <footer>
      I am a footer
    </footer>
  );
};

Footer.propTypes = {
  
};

export default Footer;