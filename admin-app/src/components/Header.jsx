import React from 'react';
import PropTypes from 'prop-types';

const Header = props => {
  return (
    <header>
      <h1>Treasure Hunt</h1>
    </header>
  );
};

Header.propTypes = {
  
};

export default Header;